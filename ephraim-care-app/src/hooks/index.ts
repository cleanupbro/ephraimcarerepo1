/**
 * @file Hooks Barrel Export
 * @description All custom React hooks
 * @searchable hooks, useComfortMode, useState
 *
 * Usage: import { useComfortMode } from '@/hooks'
 */

export { useComfortMode } from './use-comfort-mode';
